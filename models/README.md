# Models

The QRS detector weights are **not** distributed through this repository. Download
them separately and place the `.keras` file in this directory before running the
pipeline.

Expected filename:

```
models/QRS_detector_125Hz_080525.keras
```

Everything else in this folder is ignored by git.
