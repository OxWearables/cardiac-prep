# Documentation build only. The pipeline's own dependencies are not installed
# here - conf.py mocks them - so this stays small and the build stays fast.
sphinx>=7.0
sphinx-rtd-theme>=2.0
myst-parser>=2.0

# Needed at build time: the configuration reference page is generated by
# reading config.yaml and the Config dataclass, and config.py imports yaml.
PyYAML>=6.0
